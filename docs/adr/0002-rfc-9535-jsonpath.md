# Use RFC 9535 JSONPath

The X-Validations Contract uses RFC 9535 as its normative JSONPath language. The Validation Engine must preserve currently supported queries while converging on a single conforming implementation that provides both matched values and structured locations.
