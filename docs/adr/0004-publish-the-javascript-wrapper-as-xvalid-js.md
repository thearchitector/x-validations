---
status: superseded by ADR-0006
---

# Publish the JavaScript wrapper as xvalid-js

`xvalid` is the canonical Runtime Wrapper name, but the unscoped npm name is already owned. The JavaScript Runtime Wrapper is therefore published as `xvalid-js`, matching its Cargo crate and the package name generated by `wasm-pack` without a custom post-build rewrite.
