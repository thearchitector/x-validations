# xvalid-py

Native Python runtime bindings for x-validations schemas.

Install this package directly when you only need runtime validation:

```bash
pip install xvalid-py
```

The module exports `xvalid.xvalidate` plus structured exception types such as
`xvalid.XValidationError`.
